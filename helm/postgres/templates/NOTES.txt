PostgreSQL has been installed.

Service DNS:
  {{ include "postgres.serviceName" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

Connection URL for the Lumiere backend:
  postgresql://{{ .Values.auth.username }}:<password>@{{ include "postgres.serviceName" . }}:{{ .Values.service.port }}/{{ .Values.auth.database }}?schema=public

Check status:
  kubectl -n {{ .Release.Namespace }} rollout status statefulset/{{ include "postgres.statefulsetName" . }}
